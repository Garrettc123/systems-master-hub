# ===========================================================================
# GARCAR ENTERPRISE — VAULT TEMPLATE
# AutoKey System v1.1.0 | systems-master-hub
# ===========================================================================
# INSTRUCTIONS:
#   1. Copy this file: cp vault/.vault.env.template vault/.vault.env
#   2. Fill in ALL values below (no blanks in production)
#   3. Run: bash vault/vault-setup.sh
#   4. NEVER commit .vault.env — it is in .gitignore
#   5. Or add the same keys directly here:
#      https://github.com/Garrettc123/systems-master-hub/settings/secrets/actions
#   6. Then run the Autokey Propagator workflow with confirm = PROPAGATE
# ===========================================================================

# ---------------------------------------------------------------------------
# ORCHESTRATION PLANE
# Used by: systems-master-hub, neural-mesh, wealth-agent, lead-agent
# ---------------------------------------------------------------------------
GHPAT=
GITHUB_TOKEN=
LINEAR_API_KEY=
SLACK_WEBHOOK_URL=

# ---------------------------------------------------------------------------
# REVENUE ENGINE (WEALTH LOOP) — CRITICAL
# Used by: agents/wealth_loop.py, lead_agent, Apollo → Stripe pipeline
# Without APOLLO_API_KEY the wealth agent exits with BLOCKED
# ---------------------------------------------------------------------------
APOLLO_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_AGENCY=

# ---------------------------------------------------------------------------
# SHARED APP / LEDGER PLANE
# Used by: all stateful repos + agent_runs / agent_genomes tables
# SUPABASE_SERVICE_KEY = service_role key (NOT the anon key)
# ---------------------------------------------------------------------------
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
APP_URL=
APP_BASE_URL_PAYMENTS=
APP_BASE_URL_RHNS=
APP_BASE_URL_ATLAS=
APP_BASE_URL_ZEUS=

# ---------------------------------------------------------------------------
# MULTI-MODEL SUBSTRATE (MultiModelRouter)
# Missing keys = graceful degrade (provider simply not registered)
# ---------------------------------------------------------------------------
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
GOOGLE_API_KEY=
PERPLEXITY_API_KEY=
HUBSPOT_API_KEY=

# ---------------------------------------------------------------------------
# DEPLOY PLATFORM PLANE
# ---------------------------------------------------------------------------
RAILWAY_TOKEN=
VERCEL_TOKEN=
RENDER_API_KEY=
DATABASE_URL=
REDIS_URL=
NEXT_PUBLIC_APP_URL=

# ---------------------------------------------------------------------------
# AUTOKEY ROTATION TRACKING
# These are auto-populated by rotate-secrets.sh — do not set manually
# ---------------------------------------------------------------------------
# AUTOKEY_LAST_ROTATION=
# AUTOKEY_ROTATION_COUNT=
